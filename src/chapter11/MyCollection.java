package chapter11;

/**
 * <Collection 인터 페이스의 메서드>
 *     크게 3가지 기능으로 나뉜다. (추가, 검색, 삭제)
 *
 *     추가
 *          add()
 *          addAll()
 *     검색
 *          contains()
 *          containsAll()
 *     삭제
 *          remove()
 *          removeAll()
 *          retainAll()
 *
 *     isEmpty() 비어있는지 확인
 *     size() 저장된 객체의 갯수를 반환
 *
 */

/**
 * <List>
 *     ArrayList, LinkedList -> 핵심
 *
 */

/**
 * <Set>
 *     HashSet, TreeSet -> 핵심 집합과 관련된 메서드(Collection에 변화가 있으면 true, 아니면 false반환
 *
 */

/**
 * <Map>
 *     순서x, 중복(키x, 값o)
 *     (key, value)쌍으로 저장
 *     HashMap, TreeMap -> 핵심
 *
 */

public class MyCollection {

}
