package classpractice;

public class CarExam {
    public static void main(String[] args) {
        Car c1 = new Car();
        Car c2 = new Car(); // new가 생성자인데 이 생성자를 통해 객체를 만들라는 의미
        //메모리에 만들어진 객체 = 인스턴스

//        c1.name = "소나타";
//        c1.number = 1111;

//        c2.name = "아반떼";
//        c2.number = 2222;

//        System.out.println(c1.name + c1.number);

//        System.out.println(c2.name + c2.number);

        /**
         * 차의 이름과 번호가 필드
         * 차는 이런 이름과 번호판이라는 속성을 가진다
         * 그리고 차는 앞이나 뒤로 이동을 할 수 있다. 이와 같은 객체의 행동을 정의하는 것이 메서드이다
         *
         * 매개변수(Parameter): 전달된 인자를 받아들이는 변수를 의미한다.
         * 인자(Argument): 어떤 함수를 호출 시에 전달되는 값을 의미한다.
         */
    }
}
